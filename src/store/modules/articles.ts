// Initial State
const state = {
    all: [],
};

// Getters
const getters = {
};

// Actions
const actions = {
};

// Mutations
const mutations = {
};

export const moduleArticles = {
    state,
    getters,
    actions,
    mutations,
};
